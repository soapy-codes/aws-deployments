{
  "name": "soapy-codes",
  "version": "1.0.0",
  "workspaces": [
    "frontend",
    "infrastructure",
    "packages/shared-types",
    "packages/lambda/translate"
  ],
  "scripts": {
    "frontend": "npm run dev --workspace frontend",
    "deploy": "cd infrastructure; npx cdk deploy --require-approval never ApiStack --profile development"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.743.0"
  }
}
