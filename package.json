{
  "name": "soapy-codes",
  "version": "1.0.0",
  "workspaces": [
    "frontend",
    "infrastructure",
    "packages/**"
  ],
  "scripts": {
    "frontend": "npm run dev --workspace frontend",
    "deploy": "cd infrastructure; npx cdk deploy --require-approval never ApiStack --profile development",
    "build-utils-lambda-layer": "npm run build --workspace @a2t/utils-lambda-layer"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.743.0"
  }
}
